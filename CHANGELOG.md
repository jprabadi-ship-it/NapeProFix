# リリースノート

バージョンは `メジャー.マイナー.パッチ` の3桁で表記します。
`CFBundleShortVersionString` で管理し、アプリの表記は設定画面の右下にあります。

`CFBundleVersion` はビルドのたびに増やしますが表示しません。macOS が単調増加する
ビルド番号を要求するために置いているもので、Finder の情報ウインドウで確認できます。

---

## 1.2.0 — 2026-08-06

### 追加

- **スクロールの向きを反転するスイッチ。** 上に転がしたときに下へ動いてしまう
  場合に使います。このアプリのジェスチャにだけ効くので、macOS の
  「ナチュラルなスクロール」やデバイス側のボールスクロールには影響しません。
  設定画面のスクロールタブにあります。

---

## 1.1.0 — 2026-08-01

### 追加

- **クリック時にカーソルを固定する機能。** トラックボールは指がボタンを押す・離す
  瞬間にボールがわずかに回るため、狙った位置から数ピクセルずれた場所が
  クリックされます。押している間カーソルを止め、離したあと元の位置に戻します。
  ドラッグは、押している間に一定以上動かした時点で固定を解除するので妨げません。
  設定画面に「ポインタ」タブを追加し、有効・無効、ドラッグとみなす移動量、
  離したあとの保持時間を調整できます。

---

## 1.0.1 — 2026-08-01

### 修正

- **レイヤー切替ショートカットが効かなかった問題を修正。** キーの押下と解放で
  ハンドラが2回動いており、レイヤーが進んですぐ戻っていました。設定済みの
  レイヤーが2つの場合、何も起きていないように見えます。押下時のみ処理し、
  解放は下のアプリに漏れないよう飲み込むだけにしました。
- **設定画面のレイヤー選択が、使用中のレイヤーに追随するようにした。**
  ショートカットで切り替えても選択が動かず、画面に出ている割り当てが
  今は効いていないものだと分からない状態でした。
  ドロップダウンにも「（使用中）」を表示します。
- **セットアップ画面のスクリーンショットが拡大できなかった問題を修正。**
  シートは親ウインドウより広くできないため、幅640の設定ウインドウに収まる
  大きさまで縮められていました。独立したウインドウで開き、倍率は画像と
  ディスプレイから計算してピクセル等倍にします。

### 変更

- **メニューの並びを、クリックする項目が上に来るよう入れ替えた。**
  メニューは下に開くので、上ほどカーソルに近くなります。
- スクロール量の下限を 1 から 3 に引き上げた。1 行では動いていないように見え、
  設定を戻す手がかりもないため。既定値に戻すボタンも追加。
- 設定画面と README に、レイヤー切替キーの選び方を追記。
  アプリは入力元のデバイスを区別できないため、登録したキーは
  システム全体で横取りされます。その前提での選択材料を示しています。
- セットアップタブに、ほしい物リストへのリンクを追加。

---

## 1.0.0 — 2026-08-01

初回リリース。Keychron Nape Pro のトラックボールジェスチャを macOS 側でカスタマイズします。

### 機能

- **ジェスチャ4方向への割り当て**
  スクロール上下、デスクトップ切り替え、Mission Control、アプリケーションウインドウ、
  戻る、進む、なし、から選択できます。
- **任意のキーボードショートカット**
  4方向それぞれに好きなキーの組み合わせを記録して送出できます。
  文字ではなくキーコードで保存するため、入力ソースを切り替えても同じ物理キーが送られます。
- **レイヤー 0〜7**
  4方向の割り当てを8組持てます。切り替えは設定画面、メニュー、
  または登録したショートカットから行えます。ショートカットを Launcher で
  Nape Pro のボタンに割り当てると、デバイスのレイヤー機能に近い操作感になります。
  何も設定していないレイヤーは切り替え時に飛ばします。
- **回転補正**
  オクタシフトがリセットされてジェスチャの向きがズレたときに、90°刻みで合わせられます。
  Launcher を開き直す必要はありません。
- **加速つきスクロール**
  短い間隔で続けて転がすほど1回あたりの量が伸びます（既定 8行 → 最大32行）。
  量、増分、上限、連打とみなす間隔はすべて設定画面から変更できます。
- **設定画面**
  ジェスチャ、スクロール、セットアップの3タブ。
  Launcher 側に必要な設定を、正しい状態のスクリーンショットつきで表示します。
  初回起動時に自動で開きます。
- **メニューバー常駐**
  よく使う「90°回す」「次のレイヤーへ」はメニューから1クリックで届きます。
- **ログイン時に起動**

### 動作条件

- macOS 14 以降
- アクセシビリティ権限（許可すると数秒で自動的に有効になります。再起動は不要です）
- Keychron Launcher 側の設定（詳細は README または設定画面のセットアップタブ）

### 既知の制約

いずれもデバイスまたは macOS 側の仕様によるもので、アプリ側では解決できません。

- **ジェスチャは4方向のみ。** ファームウェアが斜めを区別して送りません。
- **バッテリー残量を取得できない。** デバイスが HID でバッテリーの UsagePage を
  公開していません。本体の LED でしか確認できません。
- **デバイス側のレイヤーとは連動しない。** 届くのがキーコードだけで、
  レイヤー情報が乗ってこないため、アプリ側で独自にレイヤーを持っています。
- **Launcher で修飾キーを使えない。** 修飾キーを付けると、ジェスチャ1回ごとに
  その修飾キーが押されて離され、連続で転がすと連打になります。
  `flagsChanged` が文字キーより先に届くため、アプリ側では判別も除去もできません。
- **Pause と Scroll Lock は割り当てに使えない。** Pause は macOS でキーコードを持たず、
  Scroll Lock は F14 として輝度ダウンに横取りされます。
- **公証していない。** Developer ID Application 証明書がないため、別の Mac では
  Gatekeeper に止められます。macOS 15 以降は右クリック →「開く」では回避できず、
  システム設定 → プライバシーとセキュリティ の「このまま開く」を使うか、
  `xattr -dr com.apple.quarantine` で隔離属性を外す必要があります。
